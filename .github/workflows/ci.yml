name: Build and Deploy WebAssembly to GitHub Pages

on:
  push:
    branches:
      - main  # Adjust to your branch as necessary

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        override: true

    - name: Build WebAssembly
      run: |
        chmod +x ./build-for-web.sh
        ./build-for-web.sh ndarray

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: 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
        publish_dir: ./inference/pkg  # Adjust this to your output directory
